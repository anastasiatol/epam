
const initialState = {
    movies: []
}

export function movieCollectionReducer (state = initialState, action) {
    switch (action.type) {
        case "GET_MOVIES_FROM_SERVER":
            
        default:
            return state;
    }
}